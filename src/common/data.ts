export class Data{
    constructor(
        public id:number,
        public name:string,
        public price:number,
        public image:string,
     
        ){
    return new Data(id, name, price, image);}
}